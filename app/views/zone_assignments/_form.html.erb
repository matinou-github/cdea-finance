<%= form_with(model: zone_assignment) do |form| %>
  <% if zone_assignment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(zone_assignment.errors.count, "error") %> prohibited this zone_assignment from being saved:</h2>

      <ul>
        <% zone_assignment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id %>
  </div>

  <div>
    <%= form.label :assigned_by_id, style: "display: block" %>
    <%= form.number_field :assigned_by_id %>
  </div>

  <div>
    <%= form.label :zone_id, style: "display: block" %>
    <%= form.text_field :zone_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
